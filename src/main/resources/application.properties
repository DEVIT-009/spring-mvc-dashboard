# =============
# MYSQL
# =============
spring.datasource.url=jdbc:mysql://localhost:3306/spring_dashboard_mvc_db?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=Devit009

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ============
# JPA settings
# ============
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==========
# Project
# ==========
spring.application.name=dashboard-mvc

spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# =========================
# Custom Port
# =========================
server.port=9000

# =====================
# Upload image location
# =====================
app.upload.dir=uploads